# Implementation Plans

Generated by the improve skill on 2026-07-04, planned against commit `871bd4b`
(tip of the `style-hardening` branch). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                   | Priority | Effort | Depends on | Status |
| ---- | --------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Admin notifications (webhook / Apprise) | P1       | M      | —          | TODO   |
| 002  | Invite distribution (email + QR)        | P1       | S–M    | —          | TODO   |
| 003  | Persisted admin audit log               | P2       | M–L    | —          | TODO   |
| 004  | API tokens for headless automation      | P2       | M      | —          | TODO   |
| 005  | Admin data export (CSV/JSON)            | P3       | S–M    | —          | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- No hard dependencies. Soft overlap: if 001 and 003 both land, the `notify()`
  event sites and the audit-entry emission sites largely coincide — whichever
  lands second should emit through both channels at those sites (see each
  plan's Maintenance notes).
- 004 expands the security surface (bearer auth). Its first STOP condition
  requires the maintainer to explicitly confirm they want a public API
  surface before execution.

## Findings considered and rejected

- **Seerr email-sync dead path**: fixed directly in the `style-hardening`
  branch (the no-op `updateSeerrUserEmail` chain was removed) — no plan needed.
- **PWA activation**: already wired and used (`vite.config.ts` VitePWA +
  registration hook) — not a gap. Web push would fold into plan 001 later.
- **More locales**: en+de exist with compile-time parity; adding languages is
  translation work, not a code change.
- **App-level RBAC / custom admin roles**: admin status derives from Jellyfin;
  an app-level role layer would fight that model. The real multi-admin need is
  attribution, covered by plan 003.
- **Referral / user-generated invites**: deliberately abandoned by the
  maintainer — do not re-propose.
- **Explicit return-type annotation sweep**: most annotations sit on exported
  functions, which the style guide permits — not worth a sweep.
- **Drizzle schema style churn**: existing camelCase/string-column schema is
  explicitly grandfathered by AGENTS.md; only new tables use the snake_case
  bare-column style.
